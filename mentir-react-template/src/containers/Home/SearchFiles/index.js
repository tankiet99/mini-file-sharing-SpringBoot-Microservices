import React from 'react';
import FileCards from '../../../components/FileCards';

export default function SearchFiles(props) {
    return (
        <React.Fragment>
            <FileCards isSignin={false}/>
        </React.Fragment>
    )
}